import { AddmoreDirective } from './addmore.directive';

describe('AddmoreDirective', () => {
  it('should create an instance', () => {
    const directive = new AddmoreDirective();
    expect(directive).toBeTruthy();
  });
});
